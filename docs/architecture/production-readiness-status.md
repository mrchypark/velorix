# Production Readiness Status Matrix

Reviewed on 2026-05-05 for the Velorix 1.0 production-readiness track.

This matrix separates current bootstrap evidence from 1.0 required evidence.
It summarizes the architecture document set; it is not an implementation audit
or a claim that Velorix is production-ready today.

Status values are limited to `complete`, `partial`, and `missing`.

Velorix 1.0 also includes the cross-cutting API serving scope in
`api-serving-scope-v1.md`: governed endpoints, parameter validation,
response-shape contracts, OpenAPI-compatible catalog metadata, query/cost
policy linkage, and immediate-response paths from materialized or
checkpoint-recovered state where available. This does not add a new matrix row;
the scope is enforced through the DataFusion policy, table registry, Feldera
artifact registry, benchmark gate, S3-compatible tests, and Kubernetes operator
rows.

| Contract | Current Evidence | 1.0 Required Evidence | Status | Blocking Tasks |
| --- | --- | --- | --- | --- |
| ingest | `storage-contract.md` defines durable object-store acknowledgement semantics and stable `v1/ingest` keys; `ingest-envelope-v1.md`, `ingest-admission-contract.md`, and `ingest-storage-format-decision.md` define the Arrow IPC envelope, digest, and admission rules. Current runtime replay uses validated Arrow ingest envelopes and rejects legacy JSON `DeltaBatch` bytes under `v1/ingest`; JSON `DeltaBatch` remains only as bootstrap/internal checkpoint-state compatibility and prototype scaffolding. `IngestLog::append_catalog_validated_envelope` and `append_catalog_validated_envelope_single_writer` now read the persisted relation catalog before durable append, reject missing catalogs, stale schema fingerprints, and Arrow batch schema mismatches before committing `v1/ingest` objects, and preserve idempotent same-digest retry. `IngestAdmissionCoordinator` adds process-local catalog-aware write-coordinator evidence: concurrent overlapping ranges for one stream/partition admit exactly one append, adjacent ranges both append, and same-digest retries remain duplicate/idempotent. The coordinator now records range-scoped durable serialized admission evidence under the Velorix-owned `v1/ingest-admission` namespace before the canonical ingest batch append; separate namespace capability validation covers `ingest-admission`, local tests show a separate coordinator rejects a range already reserved by durable admission evidence before any batch object is committed, and the storage integration suite now spawns child OS processes against one `LocalFileSystem` authority root or an env-gated S3-compatible authority prefix to show local and floci same-host serialized cross-process overlap-conflict evidence plus adjacent-range append evidence. This is storage plumbing for deployed admission, not a deployed distributed admission index or multi-pod guarantee. `IngestAdmissionCoordinator::new_checked` now requires the shared startup capability object and internally validates `v1/ingest`, `v1/ingest-admission`, and `v1/relations`, and the Kubernetes startup authority can construct an `IngestAdmissionCoordinatorProvider` only after `ValidatedOperatorAuthority` carries complete authoritative namespace evidence. Checked production recovery now uses strict admitted replay for replay-window batches: each replayed `v1/ingest` object must have matching `v1/ingest-admission` evidence for the deterministic keys, payload digest, relation id/version, schema fingerprint, and admission mode, while checkpoint-covered pre-admission batches are skipped and admission-only crash-window reservations are not replayed as data. The ingest admission contract now defines the admission-before-batch orphan inspection and repair/expiry procedure: orphan reservations remain reserved by default, matching batches classify as committed only after envelope digest and relation metadata match the admission record, metadata conflicts classify as corrupt conflicts, and expiry now has a digest-bound durable Velorix-owned decision record read during coordinator restart reconstruction, rejects stale ordinary retries as `admission_expired`, and the checked Kubernetes ingest-admission provider exposes a startup preflight that reconstructs active admissions before coordinator use and fails closed on malformed `v1/ingest-admission` state or committed-batch/admission mismatches; the vind Kubernetes gate now runs this provider startup preflight plus a run-local orphan expiry/restart reconstruction check in a live Kubernetes context and records `kubernetes_ingest_admission_startup_preflight` and `kubernetes_ingest_admission_run_local_expiry_restart` evidence. This remains local vind/run-local authority evidence until the deployed writer path calls the preflight before live writer cutover and broader multi-writer race evidence exists. The runtime query, persisted-query, recovery, local-recovery, local benchmark, S3-compatible benchmark, and env-gated S3 runtime recovery fixtures now create the relation catalog before ingest and use the catalog-aware append path for normal ingest; the runtime recovered-query fixture, persisted-query recovered-runtime fixture, catalog-backed runtime recovery fixture, local recovery E2E harness, local PR-smoke benchmark, S3-compatible benchmark, and env-gated S3 runtime recovery harness route normal ingest appends through the process-local coordinator. A production source contract test now rejects new `crates/*/src`, top-level `benches/*.rs`, and top-level `tests/**/*.rs` callers of bootstrap-only ingest append/replay APIs and unchecked `IngestLog::new` construction outside the storage owner and explicit bootstrap recovery helpers, and also asserts those production-like harnesses construct the coordinator and route direct catalog-aware appends through its helper, including the runtime, persisted-query, and catalog-backed recovery fixtures; the deliberate local-recovery JSON `DeltaBatch` rejection fixture, lower-level storage internals, relation-drift/malformed-batch fixtures, and the unchecked runtime recovery branch that converts validated replay envelopes through the relation catalog remain allowed. The production-readiness report now requires explicit `ingest_status` evidence with both `catalog_backed_ingest_admission` and `deployed_ingest_admission`, matching this release-status row. The single-writer variant also keeps visible committed-range overlap rejection for serialized admission while distributed multi-writer overlap guarantees remain unadvertised. | Deterministic Arrow IPC envelope framing, corruption checks, relation-catalog-backed durable admission, local write-coordinator range-admission evidence, durable idempotency/range-admission evidence, checked production replay admission enforcement, and removal or explicit migration gating for JSON `DeltaBatch` production paths. | partial | Complete an Oracle-reviewed durable admission fence or partition admission index design before claiming true concurrent cross-process range safety, then wire the deployed writer/coordinator authority path to call the checked ingest-admission provider startup preflight before serving live writers and add floci/S3-compatible true concurrent overlap-race plus crash/restart evidence and vind/Kubernetes multi-pod overlap/adjacent/crash/restart/leader-handoff evidence, prevent new JSON `DeltaBatch` durable ingest paths, and keep expanding production caller coverage as new ingest surfaces are added. |
| relation catalog | `relation-contract-v1.md` defines create-only `v1/relations/{relation_id}/versions/{relation_version}.relation.json`, schema fingerprints, DataFusion registration, Feldera validation, and incremental adapter identity. `crates/velorix-storage` persists and reads validated catalog objects by `(relation_id, relation_version)` with idempotent duplicate creates, same-key conflict rejection, stored-body identity checks, strict stored JSON parsing, durable object-store capability gating, and checked admission for the closed 1.0 sum/count adapter scope. Current code also derives typed DataFusion schemas/registration and Feldera input schemas from the catalog, rejects unsafe DataFusion registration names such as `input` and qualified/reserved names, can recover by reading the catalog record before replay, rejects ingest relation-id/version/fingerprint mismatches during recovery, and tests ingest/DataFusion/Feldera identity alignment. Local recovery success fixtures now use persisted relation-catalog-record recovery or explicit relation-catalog selected-checkpoint recovery instead of the bootstrap raw recovery helper, leaving raw recovery only in deliberate legacy/negative cases. `crates/velorix-core` now owns catalog-backed sum/count incremental-input adapters: the legacy/generic single-key/single-value/single-Int64-weight adapter validates relation identity, adapter id, catalog Arrow schema, and non-null key/value/weight columns before converting decoded Arrow record batches to relation-version-scoped `DeltaBatch` values, while a separate row-key sum/count adapter keeps the single value-column contract and encodes multi-column primary keys as deterministic JSON objects keyed by stable catalog column id. Local relation tests prove the generic adapter id reads catalog semantic roles and non-`orders` column names for a non-`orders` relation fixture, accepts Boolean, UTF-8, JSON-as-UTF-8, dictionary-encoded UTF-8, Int64, finite Float64, Date32, TimestampNanosecond, and schema-bound Decimal128-as-canonical-string primary keys plus finite Float64 and schema-bound Decimal128-as-canonical-string value columns, rejects Decimal128 precision/scale drift, keeps the legacy orders-sum-count adapter id as a compatibility alias, and still fails closed for unsupported adapters, malformed JSON keys, multi-column keys under scalar single-key adapter ids, or multiple value columns. The row-key adapter now accepts multi-column primary-key catalog batches without changing existing scalar single-key output. Decimal128 strings are claimed only inside the catalog-backed adapter/replay context; the sum/count consumer now selects an exact Decimal128 aggregation mode for known catalog-backed sum/count adapters with exactly one Decimal128 value column, consumes only canonical fixed-scale Decimal128 strings, emits aggregate state with string `sum` and numeric `count`, preserves the legacy integer aggregate state shape, and proves replay plus checkpoint hydration for Decimal128 value relations. Runtime recovery calls the generic helper after envelope decode instead of extracting columns itself, including catalog-backed replay for Boolean, JSON-as-UTF-8, dictionary-encoded UTF-8, Int64, finite Float64, Date32, TimestampNanosecond, Decimal128 primary-key, exact Decimal128 value, and row-key multi-column primary-key relations; Float64 value-column adapter conversion remains local adapter coverage rather than claimed sum/count runtime aggregation. Checkpoint recovery now validates the sum/count adapter id and single value-column shape before engine construction, and checked relation-catalog create/read admission rejects unsupported/future adapter ids or multi-value adapter shapes, so unsupported/future adapters or multi-value catalogs cannot activate through catalog admission, checkpoint-only recovery, or recovered-query paths. Direct production recovered-query execution now plans the caller SQL against the recovered materialized-view schema after shared concurrency admission and before opening the checked relation catalog and recovery path; production persisted recovered-query reads also revalidate the stored recovered-view SQL before checked recovery, so malformed ad hoc or persisted recovered-query SQL cannot force production recovery first. The bootstrap recovered materialized-view helper now also reads the persisted relation catalog record before recovery, and a regression test proves the helper fails closed when that catalog record is missing. The local PR-smoke benchmark recovery workload now probes authoritative startup capabilities and uses checked relation-catalog-record recovery before measuring checkpoint recovery. A production source contract now rejects new `crates/*/src`, top-level `benches/*.rs`, and top-level `tests/**/*.rs` callers of direct raw bootstrap recovered-runtime wrappers (`recover_bootstrap`, `recover_bootstrap_with_owner`, and `recover_bootstrap_from_published_checkpoint_version`) and rejects top-level benchmark/E2E callers of unchecked catalog-record recovery outside the recovery API owner, the default recovered-query helper's catalog-record wrapper, local-admin CLI allowance, and narrow local recovery fixtures that intentionally assert raw/bootstrap rejection behavior; the raw no-catalog recovered-query allowance has been removed. Production table scans now register Parquet snapshots under the catalog DataFusion table name with the catalog-derived Arrow schema instead of the bootstrap `input` table. The production-readiness report now requires explicit `relation_catalog_status` evidence with `relation_catalog_record`, `relation_catalog_registry`, `relation_catalog_closed_adapter_scope`, and `relation_catalog_unsupported_adapter_fail_closed`, matching this release-status row. | Relation-version pinning, typed DataFusion registration, Feldera artifact fingerprint rejection, and replay/recovery through catalog-backed typed sum/count adapters for the closed 1.0 adapter ID set, with unsupported/future adapter IDs, row-shaped adapters, and multi-value adapter shapes rejected fail-closed before catalog admission, checkpoint hydration, or engine construction. | complete | none |
| object-store capability | `object-store-capabilities-v1.md` defines `AuthoritativeObjectStoreCapabilitiesV1`, required namespaces, fail-closed startup, shared registry, and dev/local fallback limits. Current code validates declared namespace profiles, including relation catalog, exposes per-namespace backend/missing-capability diagnostics, requires capability-backed registry entries for production table scans, and has runtime probes that build an `AuthoritativeObjectStoreCapabilitiesV1` by checking create-only, read-after-write, list-after-write, and complete-payload visibility for every authoritative namespace. Namespace coverage now includes the additional Velorix-owned durable prefixes for checkpoint index, checkpoint lifecycle, checkpoint retention, checkpoint recovery, GC runs, persisted queries, and query policies. The runtime storage registry can now register a production DataFusion scan store either by probing a Velorix authority object store directly or by reusing already-probed startup `AuthoritativeObjectStoreCapabilitiesV1` evidence, while still rejecting incomplete or weak namespace profiles before registration. Runtime production recovered-query, checked latest recovery, checked selected-checkpoint recovery entrypoints, local-admin selected-checkpoint recovery, local checkpoint inspection, local GC planning, and local GC execution now require complete `AuthoritativeObjectStoreCapabilitiesV1` startup evidence before opening checked relation-catalog, checkpoint, ingest-log, or checkpoint-admin/GC authority paths. The unchecked runtime recovery entrypoints and recovered-query helpers are now explicitly bootstrap-named, production source contracts reject production callers that route through those bootstrap helpers instead of the capability-backed production recovered-query APIs, and the recovery API contract requires public non-bootstrap recovery methods to take startup capability evidence and rejects direct unchecked authority construction or bootstrap-recovery delegation in those method bodies. Checked production SlateDB recovery now opens the state store only through the shared startup capability object and requires both `checkpoint` and `state` namespace evidence before opening SlateDB. The env-gated S3-compatible benchmark `slatedb_state_reopen` workload now reuses already-probed startup capability evidence for both the initial SlateDB open and reopen, so it cannot emit that evidence through unchecked SlateDB opens. Production leased checkpoint publishing now requires the checked leased-publisher constructor, which reuses that checked SlateDB publisher path and rejects unchecked construction plus missing or weak `output` or `ownership` namespace evidence before production publication can proceed. The production persisted-query recovery wrapper now opens the persisted-query catalog with the shared startup capability object before reading persisted query specs under `v1/queries`, so malformed or missing query catalog objects cannot mask missing query namespace evidence. Production persisted table and view APIs now require explicit startup capability evidence before opening checked table, query-policy, and persisted-query catalog readers, so missing or weak `TableCatalog`, `QueryPolicy`, or `Queries` namespace evidence wins before malformed table, policy, or view-query catalog bodies; they still reuse per-table storage registry capability evidence for relation-catalog and scan-location checks, so missing production registry capabilities win before malformed relation-catalog bodies. Production view execution now reuses already validated table, relation, and policy objects instead of re-reading table or policy specs after validating the stored view SQL. The runtime Feldera artifact registry can now be constructed from the same validated startup capability object and fails closed when `artifact_catalog` namespace evidence is missing or weak before opening the checked artifact metadata registry; direct Feldera/DBSP execution remains disabled. Foyer-backed `HybridLocalCache::open_production` now also requires the same validated startup capabilities before it can fetch through object storage, while the unchecked constructor remains bootstrap/dev-only. `crates/velorix-k8s` also exposes a narrow operator startup guard that probes the configured authority object store, carries the validated `AuthoritativeObjectStoreCapabilitiesV1` evidence into the production relation-catalog snapshot provider, uses the checked relation-catalog and checkpoint readers for readiness snapshots, relation-bounds internal checkpoint evidence by stream plus relation identity, fails closed instead of reporting `latest_published_checkpoint` from stream-only checkpoint manifests while manifest v1 lacks relation identity, can construct the worker-shard ownership epoch-store bridge from the same validated startup evidence for the `ownership` namespace, and now has an `OperatorAuthorityStartupComponents` that constructs the production relation snapshot provider, ingest-admission startup provider, and worker-shard epoch store from one validated operator authority without treating Kubernetes status as durable authority. Tests reject a fake backend that silently overwrites `PutMode::Create` requests and report the failing namespace, and local runtime recovery, recovered-query, persisted recovered-query, table/view query, Feldera artifact registry, cache success fixtures, checked SlateDB recovery fixtures, production leased-checkpoint publisher fixtures, checked worker-shard epoch-store fixtures, local checkpoint inspection/GC admin fixtures, local PR-smoke benchmark ingest/checkpoint/relation-catalog/recovery/SlateDB-reopen setup, env-gated S3 benchmark ingest-admission setup, the env-gated S3 runtime recovery harness, and the env-gated S3 production table-query harness now exercise complete startup capabilities before checked catalog/checkpoint/artifact/cache/ownership/ingest-admission construction, including relation-catalog capability evidence before catalog-aware admission can read `v1/relations`. The env-gated storage S3-compatible harness now runs the authoritative probe against the configured backend/prefix and validates startup capabilities for every authoritative namespace. | Production startup probes for every authoritative namespace, rejection of overwrite-only/local emulation in production, diagnostics, and shared startup-capability reuse for storage, DataFusion, SlateDB, Foyer, Feldera artifact registry, leased-checkpoint publishing, checkpoint/GC admin surfaces, and Kubernetes watch/status authority paths. | partial | Wire the shared startup capability object into broader deployment/runtime construction beyond the currently checked production table/view scans, recovered-query startup, persisted-query catalog reads, SlateDB checked recovery, Feldera artifact registry construction, Foyer cache construction, production leased-checkpoint publisher construction, k8s worker-shard epoch-store construction, and the narrow k8s snapshot provider gate. |
| ownership | `partition-ownership-protocol-v1.md` and `storage-contract.md` define object-backed epoch records, owner claims on state/output/manifest writes, stale-owner checks, and the Kubernetes Lease coordination boundary. Current code has storage-side durable epoch record checks, a pure fail-closed production ownership backend gate that rejects in-memory/dev leases and missing durable epoch record support, and a `velorix-k8s` Kubernetes Lease adapter with fake-client tests for acquire, renew, read, release, holder mismatch, stale epoch, invalid Lease bodies, mismatched returned Lease keys, and update conflicts. The adapter persists lease-derived `OwnershipEpochRecord` values through the storage create-only API while keeping production publication gated on the durable record; runtime production publication and the worker-shard ownership bridge both reject a lower Kubernetes grant after a newer durable epoch record before allowing state/output/manifest writes or worker start. The worker-shard watch/event path now requires a fallible operator-provided command sink for reconciled `StartWorker`, `StopWorker`, lease acquisition, and durable epoch-record commands instead of silently dropping bridge output, and exposes a typed `WorkerShardCommandExecutor` helper plus concrete process-backed and Kubernetes Pod-backed executors that execute reconciled `StopWorker`/`StartWorker` commands in order while ignoring `AcquireLease`/`PersistEpochRecord` because reconcile already applied those authority-side effects. A narrow event/watch helper can now bind reconciled worker-shard output directly to a command executor, so an operator runtime can wire the Pod executor without bypassing durable lease/epoch authority. The Pod executor uses deterministic owner/epoch pod names, injects owner context into the worker container, treats duplicate start and missing stop as idempotent, and is covered by local fake kube-API tests plus env-gated live worker-shard tests that create, read, and delete a worker Pod after lease/epoch reconciliation and create a worker Pod through the real `VelorixWorkerShard` watch loop. The env-gated live Lease test exercises acquire, renew, current, and release against a configured Kubernetes API such as vind/vCluster. | Durable epoch-record creation after lease acquisition, production rejection of claims without epoch records, stale-worker rejection after lease loss, worker-shard command emission through a fallible watch-loop command boundary plus local fakeable, process-backed, Pod-backed, event-to-executor wiring boundaries, and env-gated live Kubernetes Lease/Pod executor/watch-loop evidence. | partial | Extend the Pod-backed executor path into broader operator runtime lifecycle management while keeping object storage as the durable authority. |
| checkpoint lifecycle | `storage-contract.md` defines checkpoint manifests as durable authority; `checkpoint-recovery-index-v1.md` defines latest-candidate validation, recovery modes, digest-bound `published` lifecycle status records, retention evidence records, and admin inspection. Current code writes readable published lifecycle records, writes digest-bound append-only checkpoint retention records after GC run evidence read-back for non-retained checkpoints whose payloads were deleted, can inspect manifests to report invalid future checkpoints while preserving the latest valid checkpoint for repair/read-only diagnosis, treats GC-deleted payloads on an older listed parent as invalid for that older manifest without invalidating a newer retained child whose own payloads remain available, exposes read-only local checkpoint inspection through `velorix-cli checkpoint-inspect-local`, reports digest-matched recovery-transition records for each inspected manifest in both text and JSON inspection output, and can recover read-only from an admin-selected published checkpoint version after reading the persisted relation catalog record and validating the manifest body/key/version, parent lineage, payload visibility, and matching lifecycle digest. Checked recovery now writes digest-bound append-only recovery transition evidence under `v1/checkpoint-recovery/{checkpoint_version:020}/transitions/{transition_id}.transition.json` after successful recovery, including recovery mode and replay counts; this is admin/readiness evidence, not checkpoint authority. Local admin recovery now probes authoritative object-store startup capabilities and uses checked SlateDB-backed recovery when `velorix-cli recover-local --relation-id <id> --relation-version <version> --slatedb-state-path <object-store-db-path>` is supplied, including optional `--checkpoint-version <n>`; the SlateDB path is the object-store database path, not another local root. Omitting `--slatedb-state-path` is treated as legacy raw-object bootstrap/migration recovery and requires `--allow-bootstrap-raw-state`. The production-readiness report now requires checkpoint status to include both `published_checkpoint_lifecycle_record` and `checkpoint_recovery_transition_record` evidence so a published-only lifecycle record cannot satisfy release readiness by itself. | Lifecycle transitions beyond `published`, retention metadata that affects GC, recovery transition records, compaction/cleanup integration, and admin repair/read-only recovery commands. | partial | Add lifecycle transitions beyond `published`, compaction/cleanup state, full admin repair commands, deployed recovery-transition evidence, and any manifest-deletion policy only after explicit design review. |
| state substrate | `state-substrate-contract.md` defines raw bootstrap refs versus `SlateDbCheckpointRefV1`, requires typed state refs, and forbids Velorix GC prefix-walking SlateDB internals. Current code writes optional SlateDB checkpoint metadata with database path, state key, SHA-256 digest, byte length, and creating checkpoint version; publication validates a small same-transaction SlateDB marker and payload-key readability without hashing full payloads on the publish path; SlateDB reads fail closed for raw refs, missing or mismatched metadata, digest/length mismatch, and can recover after close/reopen through the returned ref. Runtime recovery now has SlateDB-backed entrypoints for latest and selected published checkpoint recovery, including checked selected-checkpoint recovery that opens SlateDB state through validated startup capabilities, reads the persisted relation catalog, writes selected-checkpoint recovery-transition evidence, and replays only ingest batches after the manifest boundary. Runtime recovered-query execution also has production entrypoints that require a persisted relation catalog record plus SlateDB state path before exposing the recovered materialized view to DataFusion, including the persisted-query wrapper that loads stored SQL/policy but still recovers through the checked SlateDB/catalog path; the raw-state helper remains bootstrap-compatible. Local admin raw-state recovery is gated by `--allow-bootstrap-raw-state` when no SlateDB path is supplied, and `recover-local` now rejects combining that bootstrap flag with `--slatedb-state-path` so raw bootstrap and SlateDB recovery modes remain explicit and mutually exclusive. `recover-local --slatedb-state-path` now probes authoritative capabilities on the same local store before opening checked SlateDB/catalog/checkpoint recovery paths. Production fenced state writes reject raw/default state-store construction before creating raw state objects, and production fenced publication rejects raw refs. GC now releases manifest-retired SlateDB checkpoint refs through the SlateDB state store by deleting only the logical state key and Velorix marker key, while retained SlateDB refs remain readable. | Production manifests recover from SlateDB checkpoint/root handles, raw state refs are rejected without bootstrap or migration flag, mixed raw-to-SlateDB lineage is explicit and tested, and GC uses SlateDB-owned release APIs for retired checkpoint refs. | partial | Make mixed raw-to-SlateDB lineage explicit where migration needs it, extend recovery evidence beyond the current narrow state-byte handle, and design broader SlateDB retention handles only after explicit review. |
| DataFusion policy | `datafusion-resource-policy-v1.md` defines `QueryExecutionPolicyV1`, typed errors, timeout, memory, spill, scan-byte, object-request, file, and concurrency limits. Runtime query paths now centralize DataFusion `SessionContext` creation and wire `batch_size`, `target_partitions`, `memory_limit_bytes`, and `spill_limit_bytes` through DataFusion 53 `SessionConfig`/`RuntimeEnvBuilder`; timeout, output byte, scan preflight, registry-backed production scan-byte preflight, query-wide object-request metering, and concurrency evidence exists in runtime tests. Object-backed query metering now counts object-store operations before delegation, charges preflight and DataFusion reads against one request budget, records `get_ranges` bytes, and wraps all `get_opts` payloads through a stream path so `bytes_read` advances as chunks are consumed instead of when the stream or local file payload is opened. Object-backed scan preflight cancellation now has local evidence that execution timeout drops the stalled list stream and releases the shared `QueryExecutionLimiter` permit before a subsequent query uses the same limiter successfully. The same limiter instance also has local cross-surface evidence: while an object-backed query holds the only permit, a production recovered materialized-view query fails immediately with `ConcurrencyLimitExceeded` before recovery setup matters. DataFusion 53 memory exhaustion is observable as `ResourcesExhausted` for a grouped aggregate through the public object-backed Parquet query path, and DataFusion 53 spill-quota failure is locally proven at the runtime disk-manager boundary created from Velorix `spill_limit_bytes` policy. Production persisted table scans now fail closed when a catalog policy sets `max_concurrent_queries` but the caller has not supplied the shared `QueryExecutionLimiter`, and accept the same scan when the shared limiter is provided; the object-request metrics production scan path now has the same limiter-aware entrypoint and fail-closed behavior. Production persisted views now pass the same optional shared limiter through the production table path, reject missing or mismatched limiters, and keep policy enforcement in the catalog-backed table helper. Persisted production recovered-query execution now loads stored SQL/policy but uses the same production limiter-aware recovered-query helper, so a stored concurrency limit fails closed without a shared limiter and succeeds with a matching limiter instead of bypassing the pool. | Policy connected to DataFusion session config, memory exhaustion, spill/cancellation, exact object-store scan instrumentation, planning/execution timeouts, output byte limits, production table/view/recovered-query/metrics scan concurrency admission, and concurrency pools, with clear evidence for DataFusion-version-dependent memory/spill behavior. | partial | Extend spill failure evidence from DataFusion 53 disk-manager quota enforcement into reliable operator-level query evidence only where DataFusion consistently spills under bounded local inputs, and add broader tenant/global shared-runtime semantics beyond the production persisted table/view/recovered-query scan paths. |
| table registry | `external-table-surface-contract.md` and `storage-contract.md` define registry-backed production table specs under `v1/tables` and reject raw `s3://`, `http://`, and `file://` URL authority in production. Current runtime code also has a create-only durable query policy catalog under `v1/query-policy/{tenant_id}/{query_policy_id}.json`; production table specs now pin `relation_id`, `relation_version`, and `schema_fingerprint`, creation rejects mismatches and non-table DataFusion registrations against the relation catalog before writing, and query rechecks the current catalog fingerprint after tenant rejection and before policy/scan resolution. Query policy catalog admission now has explicit production table-scan create/get methods that reject default policies missing required SQL-size, timeout, output, scan, object-request, memory, or spill bounds while preserving generic bootstrap create/get compatibility. Production scans resolve the shared storage registry, require registry startup capability evidence before pinned relation-catalog reads, register only the relation catalog table name, pass the catalog Arrow schema into DataFusion Parquet registration, enforce scan-byte limits against the registered snapshot, and thread optional shared concurrency admission through the normal and metrics production table scan entry points so the legacy `input` alias is not available in production scans. Production persisted views now load stored SQL and pass table id, tenant id, storage registry, and optional shared limiter into the same production table helper instead of opening a separate scan path, with the same checked relation-catalog capability path before policy/view execution. The local PR-smoke and env-gated S3-compatible benchmark table-scan workloads now also create production query policies, probe-backed storage registry entries, and production table specs before scanning through the production table helper instead of using raw object URLs or the bootstrap `input` alias. A production source and benchmark contract test now guards common direct `crates/*/src` and top-level `benches/*.rs` callers from regressing to raw URL/bootstrap table scan APIs, unchecked `StorageRegistry::register` use, and the persisted-view bootstrap helper outside the bootstrap helper definitions. | Allowlisted object-store registry identity, tenant prefix policy, query policy linkage, relation-catalog fingerprint binding, raw URL rejection in production, bounded DataFusion scans, and production persisted table/view/benchmark scan policy enforcement through the shared registry. | partial | Wire any remaining production callers to the production table/policy helpers and keep expanding bounded-scan evidence. |
| Feldera artifact registry | `feldera-artifact-contract.md` defines `StandingViewSpec`, `FelderaCompileArtifactMetadata`, `FelderaReleaseArtifactProvenanceV1`, spec hashes, fail-closed validation, relation fingerprint checks, the release artifact trust boundary, and the storage create-only metadata registry at `v1/feldera-artifacts/{artifact_id}/sha256/{artifact_hash_hex}.artifact.json`. `crates/velorix-core` now has artifact hash verification helpers that reuse metadata/spec validation and verify supplied artifact bytes against `artifact_hash` without claiming release-pipeline provenance; it also validates release provenance identity and requires artifact id/hash, spec hash, and generated Rust ABI/crate identity to match the compile metadata. `crates/velorix-storage` persists and reads validated metadata with idempotent duplicate creates, same-key conflict rejection, and stored-body identity checks. `crates/velorix-runtime` now exposes a thin runtime facade that validates registration/selection against a supplied `VelorixRelationCatalogV1`, can require matching artifact bytes before registration, returns trusted metadata with direct execution disabled, and emits typed readiness evidence for artifact hash verification and release provenance verification, without adding a tenant/artifact-id object-key scheme. The release gate now requires reviewable Feldera spec, metadata, artifact package, and release provenance paths, runs both existing CLI validators, validates the readiness report, and uploads the generated Feldera/readiness evidence JSON. | Persisted release artifact registry mapping artifact id/hash to trusted compiled packages, build/release verification, runtime artifact selection, and state/resource/recovery gates before direct execution. | partial | Provide real release artifact/provenance inputs in the release pipeline, add any future globally unique artifact-id index only if product semantics require it, and keep runtime Feldera/DBSP execution gated until artifact, state, resource, and recovery contracts are proven. |
| benchmark gate | `benchmark-gate-v1.md` defines PR smoke, nightly S3-compatible, and release gates, required JSON metrics, V1 `workload_metrics`, regression budgets, and S3-compatible artifact requirements; it also says `local_incremental` is bootstrap evidence only. Current code validates benchmark JSON, including backend/workload pairing, required object-request metrics, and workload detail sanity checks, through `velorix-cli benchmark-validate`; `velorix-cli benchmark-gate --gate-level --backend` also requires the current V1 workload detail names, rejects placeholder current-result commits and placeholder baselines for all backends, and can emit stable JSON gate evidence with `evidence_kind=local_benchmark_gate` or `s3_compatible_benchmark_gate`, baseline/result paths, commits, workload names, and regression budget. CI, nightly, and release workflows upload that gate evidence alongside the benchmark result JSON. `local_incremental` now emits real local instrumentation details for the authoritative object-store capability probe, catalog-aware ingest envelope admission, checkpoint publication, checkpoint recovery through probed startup capabilities and checked catalog-backed recovery, production-registry-backed bounded DataFusion Parquet table scan with local authority object-store requests attributed to catalog/policy/spec/probe setup, SlateDB state write/read/reopen through public state-store APIs, GC dry-run planning through `CheckpointPublisher::plan_garbage_collection`, and local GC execution evidence through `execute_garbage_collection_plan_with_evidence` with persisted `GcRunV1` and checkpoint-retention read-back checks. The committed local PR-smoke baseline is a non-placeholder conservative threshold for schema, workload, commit-field, and gate-wiring evidence, not a production performance claim. `s3_incremental` is env-gated and emits S3-compatible JSON for the same readiness workloads against a real configured backend except this local-only GC execution workload; its capability-probe workload now records authoritative startup probe latency and object requests, its recovery workload uses the same probed authoritative namespace capabilities for checked ingest-log, relation-catalog, checkpoint-publisher, and recovery construction, and its DataFusion table-scan workload reuses the same probed startup capability object when registering the production table registry/helper path with object-request metrics instead of a raw table URL. Live S3-compatible GC execution evidence remains unclaimed. The release gate runs the S3-compatible benchmark gate before validating the submitted readiness evidence report, so claimed `s3_compatible_benchmark_gate` evidence cannot replace the benchmark command. The committed S3 baselines remain placeholders until live S3-compatible evidence is available. | Schema-validated benchmark JSON, local and S3 baselines based on real workload evidence, synthetic regression failure tests, object request metrics, and CI/release workflows that fail closed on missing or mixed backend evidence. | partial | Replace S3 placeholder baselines with measured S3-compatible evidence, add live GC execution/listing-consistency evidence outside local benchmark timing, and keep release blocked until a real S3 release baseline exists. |
| S3-compatible tests | `s3-compatible-test-harness.md` defines the live env contract and skipped-by-default behavior; `crates/velorix-storage/tests/s3_compat.rs` validates create-only conflict, read-after-write, list-after-write, range read, authoritative startup capabilities for every namespace, and best-effort cleanup against an explicitly enabled S3-compatible backend. `crates/velorix-runtime/tests/s3_compat_query.rs` is feature-gated and writes Parquet under the configured prefix, registers a production table through the probed storage registry path, stores relation catalog/query policy objects, and verifies a DataFusion aggregate query over the registered table while keeping the `object_store` 0.12 authority client and DataFusion `object_store` 0.13 scan client explicit; the same env-gated harness now also probes authoritative namespace capabilities, writes catalog-aware validated ingest envelopes plus the checked recovery relation catalog record, publishes checkpoint state/manifest objects, and recovers through `RecoveredRuntime::recover_with_owner_and_relation_catalog_record_checked` from an isolated S3-compatible authority prefix. `.github/workflows/nightly.yml` can run the env-gated live storage/runtime S3-compatible tests and generate/gate/upload a live S3-compatible benchmark artifact only after explicit opt-in and required backend configuration, and otherwise fails closed unless a benchmark JSON artifact is supplied for the nightly S3 benchmark gate. `scripts/run-floci-s3-gate.sh` and the manual `Floci S3 Emulator Gate` workflow provide a local Floci-backed S3 emulator gate that creates a bucket, runs the same storage/runtime harnesses, optionally runs and validates `s3_incremental`, and writes reviewable local emulator evidence without replacing release-quality S3 baselines. `fsouza/fake-gcs-server` is explicitly kept out of this S3 gate because it emulates the GCS API rather than S3 REST XML. `benches/s3_incremental.rs` fails closed unless `VELORIX_S3_COMPAT=1`, real S3 config, and `--features s3-compat-tests` are present, so it does not emit local or synthetic S3-compatible benchmark JSON. The production-readiness report now requires explicit `s3_compatible_test_status` evidence with `s3_compatible_integration_harness`, matching this release-status row. `benchmark-gate-v1.md`, `object-store-capabilities-v1.md`, `storage-contract.md`, and package reviews require broader S3-compatible evidence and say local filesystem emulation is not production evidence. | Env-gated S3-compatible integration tests for capability probes, catalog-aware ingest, checkpoint publication/recovery, DataFusion scans, benchmark artifacts, and failure modes. | partial | Add measured CI/nightly result artifacts from an explicitly configured S3-compatible backend and expand failure-mode evidence. |
| Kubernetes operator | `package-review-index.md` and `third-party-first.md` define a Kubernetes-native production direction with CRDs, operator reconciliation, status, scheduling intent, worker orchestration, and Lease-based fencing. `crates/velorix-control` has a kube-free Kubernetes control-plane contract skeleton: stable serde wire shapes for desired view intent, observed-only status fields, and a side-effect-free reconcile plan that forbids worker start from status-only progress or lease-only ownership without a matching durable epoch record. `crates/velorix-k8s` owns the Kubernetes dependencies and generates namespaced CRD schemas for `VelorixDatabase`, `VelorixStream`, `VelorixTable`, `VelorixWorkerShard`, `VelorixCheckpointPolicy`, and `VelorixBenchmarkGate`, with tests for required spec fields and per-kind observed-only status shape. `VelorixWorkerShardSpec` now carries explicit `view_id` for clean lease-key mapping; this is a v1alpha1 shape update and any existing cluster objects need migration/defaulting before rollout. It also has a pure controller reconciliation core that validates stream authority and relation fingerprints from object-store-backed evidence and produces status-update actions only; stale Kubernetes checkpoint status is replaced by the authoritative evidence snapshot. A Kubernetes Lease adapter now uses `coordination.k8s.io/v1` Lease shapes behind a fakeable API boundary and writes lease-derived epoch records through storage create-only semantics without making Kubernetes the database authority. `crates/velorix-k8s` now also has a fakeable Stream status writer, a relation-catalog-backed authority snapshot provider that retains `ValidatedOperatorAuthority` capability evidence, does not report `latest_published_checkpoint` from stream-only checkpoint manifests because manifest v1 lacks relation identity, constructs production snapshots through checked relation-catalog/checkpoint readers, keeps production construction gated by `ValidatedOperatorAuthority`, a minimal kube-runtime stream watch slice that applies `VelorixStream` events by reconciling against object-store-derived evidence, a minimal kube-runtime worker-shard watch slice that maps `VelorixWorkerShard` apply/delete events into the lease plus durable epoch-record bridge, and a fakeable worker-shard ownership bridge that acquires/reads Kubernetes leases, persists durable epoch records through a production constructor backed by `ValidatedOperatorAuthority` and `ownership` namespace capability evidence, rechecks durable epoch authority, and emits worker start/stop commands only from the existing control-plane plan. The worker-shard watch path now requires those reconcile outputs to flow through a fallible operator-provided sink or the typed event/watch command-executor wiring helper, and a typed command-executor helper can run the resulting `StopWorker`/`StartWorker` commands in order while skipping already-applied lease acquire and epoch-record persistence commands; concrete process-backed and Kubernetes Pod-backed executors now run start/stop boundaries with owner context, with the Pod executor creating deterministic owner/epoch Pods, injecting owner environment, treating duplicate starts and missing stops as idempotent, and keeping object-store epoch records authoritative. Local tests prove applied events emit acquire/persist/start or stop/start commands, sink failures return a typed error, executor wiring runs only worker start/stop commands after lease/epoch authority effects, the real Pod executor sends the expected Kubernetes Pod create/delete requests through a fake kube API client, duplicate start and missing stop API responses are idempotent, executor failures return a typed error without later command execution, acquire/persist-only outputs do not call the executor, deleted events emit no command output, process commands carry owner env vars, Pod names/specs are deterministic, and production epoch-store construction persists/read-backs ownership records only after validated startup authority. Env-gated live tests now exercise real CRD create/read/reconciled-status/delete round-trips, the real Kubernetes Lease API, direct worker-shard ownership plus Pod-backed executor reconciliation, and a real `VelorixWorkerShard` watch loop that creates a worker Pod only after lease plus durable epoch-record authority; those worker-shard live paths now construct the epoch store through the validated startup-authority path. `scripts/run-vind-k8s-gate.sh` and the manual `Kubernetes vind Gate` workflow run the local vind gate end to end and produce/upload a `kubernetes-vind-evidence` JSON artifact with the cluster context, applied CRDs, tool versions, and live test set. There is still no full controller framework. | CRD schemas, operator reconciliation skeleton, status writer, fallible worker-shard command output boundary, local fakeable, process-backed, Pod-backed, fake kube-API, event/watch-to-executor wiring, env-gated live Pod executor and worker-shard watch-loop boundaries, real or env-gated Kubernetes lease adapter, startup-gated object-store authority snapshots and ownership epoch-store construction, and tests proving Kubernetes remains coordination-only while object storage stays authoritative. | partial | Add broader operator runtime lifecycle management without making Kubernetes status authoritative. |
| GC | `storage-contract.md` now documents a deterministic manifest-aware plan/execute path for Velorix-owned raw `v1/state/...` objects, `v1/outputs/...` objects, and manifest-retired `SlateDbCheckpointRefV1` state refs, retaining latest published manifests, avoiding SlateDB internal-prefix deletes, and leaving `v1/tmp/...` publish-temp attempt objects unplanned and undeleted in focused checkpoint-publish evidence. `velorix-cli gc-plan-local --json` exposes the read-only GC dry-run plan with stable `schema_version=1` JSON, `velorix-cli gc-execute-local --json` exposes local/admin execution evidence, and `execute_garbage_collection_plan_with_evidence` recomputes the policy-derived plan before deletion, rejects mismatched caller plans, writes stable `GcRunV1` JSON under `v1/gc-runs/...`, reports success only after the persisted evidence reads back with the expected schema version and `run_id`, then writes digest-bound retention evidence for non-retained checkpoints whose raw state/output payloads or SlateDB logical state refs were released. Storage now also exposes a release-evidence prerequisite verifier that re-reads a persisted `GcRunV1`, requires the run to be visible in the `v1/gc-runs` listing, and checks expected checkpoint-retention records against deleted checkpoint payloads before any future production evidence generator can rely on the run. Production-readiness evidence now requires `production_gc_run_evidence` in addition to the generic GC run and checkpoint-retention evidence kinds, and the release gate requires a separate production GC artifact bound to the readiness report deployment and authority store with non-local authority, listing-consistency, and retention-record checks, so local/admin or cross-deployment `GcRunV1` output alone cannot satisfy release readiness. Storage tests cover orphan raw state, orphan output, retired SlateDB state release, retained SlateDB state readability, referenced-object retention, temp attempt exclusion, SlateDB prefix exclusion, unsafe caller-plan rejection, policy/plan mismatch rejection before deletion, retention evidence after successful run evidence read-back, no retention evidence when run evidence read-back fails, digest-mismatched retention evidence ignored by admin inspection, stale-plan rejection when a new manifest references a candidate, fail-closed candidate discovery when `v1/state` or `v1/outputs` listing errors, execution-time manifest revalidation listing failure with no candidate deletion, read-back failure after evidence write, run-id/schema validation, serialized run evidence, listed-run verification, and matching-retention verification. | Production GC evidence for broader listing-consistency behavior, lifecycle/retention administration, and broader SlateDB retention handle policy. | partial | Add live listing-consistency evidence, a production GC evidence generator that emits `production_gc_run_evidence` only from verified live backend run/retention evidence, and broader SlateDB retention handle policy after explicit design review. |
| dependency governance | `deny.toml` defines an explicit license allowlist and source policy; CI and the release gate run `cargo deny -f json check -W unmaintained` and capture JSONL diagnostics; `dependency-governance.json` records the declared MSRV policy, machine-validated package review records for DataFusion/object storage/Kubernetes/SlateDB/Foyer/Feldera artifact subjects, and owner/expiry/reason/replacement-plan/promotion-rule fields for the current duplicate and unmaintained warning exceptions; `velorix-cli dependency-governance-validate` checks the manifest, required package-review coverage, duplicate review subjects, exception promotion metadata, and machine-compares duplicate/unmaintained warning exceptions against the current `cargo deny` diagnostics in CI and the release gate. The validator can now emit stable local `local-dependency-governance-evidence.json` with `evidence_kind=dependency_governance_validated`, reviewed subjects, exception counts, cargo-deny warning counts, and `external_audit_attestation=false`; the release gate uploads it alongside `cargo-deny.jsonl` as local diagnostics, but the readiness-report release artifact validator rejects dependency-governance evidence unless `external_audit_attestation=true` and a concrete `external_audit` object names `tool="cargo-vet"`, provider, pass result, subject commit, `sha256:` manifest digest, completion time, and reviewable attestation URI, then compares the subject commit and manifest digest with the release command's `--release-commit` and `--dependency-governance-manifest`. Duplicate-version, unmaintained, and advisory warning promotion remains local governance maintenance and is not a 1.0 release blocker beyond the tracked exception metadata already enforced by the validator. CI installs Rust `1.88.0` and runs `cargo check --workspace --all-targets --locked` to enforce the declared MSRV. The production-readiness report now requires `dependency_governance_status` with `dependency_governance_validated` evidence before `production_ready=true`. | Dependency audit/vet process through `cargo-vet`, feature-flag review, replacement plans for gated packages, and tracked warning exceptions. | partial | Add a release-bound `cargo-vet` dependency-governance attestation. |
